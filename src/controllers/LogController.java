package controllers;

public class LogController {
//	public static void showLog(Exception e, String message, LogMessageTypes type) {
//		if (e != null && GeneralSettings.isShowingError) {
//			new ErrorLog(e, message).broadcastToMessageCenter();
//		}
//		if (LogMessageTypes.WARNING.equals(type) && GeneralSettings.isShowingWarning) {
//			new WarningLog(message).broadcastToMessageCenter();
//		}
//		if (LogMessageTypes.MESSAGE.equals(type) && GeneralSettings.isShowingMessage) {
//			new MessageLog(message, type).broadcastToMessageCenter();
//		}
//		if ((LogMessageTypes.MONITOR.equals(type) || LogMessageTypes.ARCHIVE.equals(type))
//				&& GeneralSettings.isShowingMonitor) {
//			new MessageLog(message, type).broadcastToMessageCenter();
//		}

//	}
}
